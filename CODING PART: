##ALL CODE IS WRIITEN IN PYTHON ;
# Automated Report Generation using ReportLab
# Reads data from a CSV file, analyzes it, and generates a PDF report

import csv
import statistics
from reportlab.platypus import SimpleDocTemplate, Paragraph, Table, TableStyle
from reportlab.lib.styles import getSampleStyleSheet
from reportlab.lib.pagesizes import A4
from reportlab.lib import colors

# -------------------------------
# STEP 1: CREATE / READ DATA FILE
# -------------------------------

csv_file = "sales_data.csv"

# Sample data file
with open(csv_file, "w", newline="") as file:
    writer = csv.writer(file)
    writer.writerow(["Product", "Sales"])
    writer.writerow(["Product A", 1500]()


